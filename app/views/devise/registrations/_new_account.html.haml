.form-group{ class: "btn-#{provider_class(provider)}"}
  %label.control-label.col-sm-3
    = provider_icon(provider)
    = provider_name(provider)
    account
  .col-sm-7
    = system_link "Link your #{provider_name(provider)} account", user_omniauth_authorize_path(provider), class: 'form-control btn btn-primary'
