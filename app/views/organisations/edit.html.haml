- content_for(:page_title) { "#{@organisation.name} Settings" }
- content_for(:sidenav) { render 'organisations/sidenav', organisation: @organisation }

%section
  .page-header
    %h3 #{@organisation.name} Settings

  = render 'form', organisation: @organisation
